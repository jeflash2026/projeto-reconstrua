import type { ReactElement } from 'react';
import EmBreve from '../../../../components/em-breve';

const Page = (): ReactElement => (
  <EmBreve
    titulo="Casos Aprendidos"
    descricao="Os atendimentos encerrados que ensinam a AHRI a decidir melhor. Visualização em construção."
  />
);
export default Page;
