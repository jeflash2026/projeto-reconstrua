import type { ReactElement } from 'react';
import EmBreve from '../../../../components/em-breve';

const Page = (): ReactElement => <EmBreve titulo="Conhecimento" descricao="Os fatos que a AHRI aprende conversando com cada cliente. Visualização em construção." />;
export default Page;
