import type { ReactElement } from 'react';
import EmBreve from '../../../../components/em-breve';

const Page = (): ReactElement => <EmBreve titulo="Evolução do Catálogo" descricao="Taxa de acerto, estratégias mais corrigidas, documentos que mais faltam e confiança média — o catálogo se medindo. Visualização em construção." />;
export default Page;
