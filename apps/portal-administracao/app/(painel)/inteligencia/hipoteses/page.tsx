import type { ReactElement } from 'react';
import EmBreve from '../../../../components/em-breve';

const Page = (): ReactElement => <EmBreve titulo="Hipóteses" descricao="As hipóteses jurídicas que a AHRI formula a partir dos fatos — sustentadas por evidência, nunca inventadas. Visualização em construção." />;
export default Page;
