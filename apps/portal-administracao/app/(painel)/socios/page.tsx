import type { ReactElement } from 'react';
import SociosPanel from '../../../components/socios-panel';

// Painel de Sócios (Decreto 2026-07-23) — cadastro por CPF, link de senha e rateio.
const SociosPage = (): ReactElement => <SociosPanel />;
export default SociosPage;
